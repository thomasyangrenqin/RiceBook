import { BirthdateDirective } from './birthdate.directive';

describe('BirthdateDirective', () => {
  it('should create an instance', () => {
    const directive = new BirthdateDirective();
    expect(directive).toBeTruthy();
  });
});
